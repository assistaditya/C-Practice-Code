#include<stdio.h>
#include<string.h>

int main()

{
    char name[1000];
    int i;
    
    printf("\n enter the name of string = ");
    scanf("%s",name);
    
    for(i=0;name[i]!='\0';i++);
    
    printf("\n The length of string = %d",i);
    
    return 0;
}
