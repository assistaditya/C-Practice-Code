#include<stdio.h>
#include<conio.h>

int main()

{
    
    int i=1,n,result;
    printf("\n Enter a number to  print a table = ");
    scanf("%d",&n);
    for(i=1;i<=10;i++)
    {
        result=n*i;
        printf("\n %d * %d = %d  ",n,i,result);
    }
    
    return 0;
}
