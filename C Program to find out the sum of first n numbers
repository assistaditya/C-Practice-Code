#include<stdio.h>
#include<conio.h>

int main()

{
    
    int i,n,sum=0;
    printf("\n Enter any number = ");
    scanf("%d",&n);
    
    for(i=0;i<=n;i++)
    {
        sum=sum+i;
    }
    printf("\n The sum of numbers = %d",sum);
    return 0;
}
